# Auto-detect text files and normalise line endings to LF on commit
* text=auto eol=lf

# Explicitly mark binary files (Git won't try to diff them)
*.png  binary
*.jpg  binary
*.ico  binary
*.woff binary
*.woff2 binary